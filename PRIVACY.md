# Privacy

Skill2 has no hosted service or telemetry. Its deterministic tools process local Skill files and local Agent session logs on the user's machine.

Skill2 does not intentionally transmit prompts, transcripts, absolute paths, credentials, or usage reports. Network access may still occur when the user's package manager fetches declared dependencies or when an Agent performs a separately authorized action.
